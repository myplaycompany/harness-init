# {project_name} — 도메인 지식

> **이 문서는 의미(semantics) 전용이다.**
>
> 코드 구조 — 심볼이 어디 있는지, 무엇이 무엇을 호출하는지, 무엇을 바꾸면 어디가
> 깨지는지 — 는 여기 적지 않는다. 문서에 박제하는 순간부터 낡기 때문이다.
> 구조는 codegraph에 물어본다:
>
> ```
> codegraph explore "<질문>"     # 관련 심볼 + 소스 + 호출 경로 한 번에
> codegraph impact <심볼>        # 이걸 바꾸면 어디가 영향받나
> codegraph callers <심볼>       # 누가 호출하나
> ```
>
> 여기에는 **코드를 아무리 읽어도 알 수 없는 것**만 적는다.
> 자세한 역할 분담은 `.claude/rules/knowledge.md` 참조.

## 프로젝트 개요

| 항목 | 내용 |
|------|------|
| 목적 | TODO: 이 시스템이 해결하는 문제 |
| 주요 사용자 | TODO: 내부 팀 / 외부 고객 / 자동화 시스템 |
| 핵심 도메인 | TODO: 예) 정산, 매칭, 분석, 인증 |

## 용어 사전

> 한국어 도메인 용어 ↔ 코드 식별자 대응. 에이전트가 "정산"이라는 말을 듣고
> `settlements` 모듈을 찾아갈 수 있게 하는 표다.

| 용어 | 코드 식별자 | 의미 |
|-----|------------|------|
| TODO | `` | |

## 슬랭 / 내부 용어

> 팀 안에서만 통하는 줄임말, 별칭, 관용 표현. 신규 입사자와 에이전트가 가장 자주
> 막히는 지점이고, 코드 어디에도 정의가 없는 유일한 지식이다.

| 슬랭 | 정식 명칭 | 의미 | 관련 코드 |
|-----|----------|------|----------|
| TODO | | | `` |

## 상태값의 의미

> Enum·상수의 **값 목록**은 아래 명령으로 언제든 정확히 뽑을 수 있다.
> 여기에 적을 것은 각 값이 **무슨 뜻이고 언제 전이하는가**다.
>
> ```
> python3 .claude/scripts/domain-extract.py . --format md
> ```

| 상태 | 값 | 의미 | 전이 조건 |
|-----|-----|------|----------|
| TODO | `` | | |

## 비즈니스 규칙

> 여러 모듈에 흩어져 있어 한 파일만 봐서는 안 보이는 규칙, 예외, 정책.

- TODO

## 외부 연동 시스템

| 시스템 | 용도 | 실패 시 영향 | 비고 |
|--------|------|-------------|------|
| TODO | | | |

## 변경 시 주의사항

> 이 코드베이스를 건드릴 때 밟기 쉬운 지뢰. 과거에 사고가 났던 지점을 남긴다.

- TODO

## 변경 이력

| 날짜 | 변경 내용 | 작성자 |
|------|----------|--------|
| TODO | 초기 작성 | |
